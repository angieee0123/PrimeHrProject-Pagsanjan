ROUTE UPDATE INSTRUCTIONS

Replace the route at line 166-212 in routes/web.php:

FROM:
Route::get('/permanent/leave', function () {

TO:
Route::get('/permanent/leave', [PermanentLeaveBalanceController::class, 'show'])

Then add this import at the top of web.php after existing imports:
use App\Http\Controllers\PermanentLeaveBalanceController;

The controller is already created at:
app/Http/Controllers/PermanentLeaveBalanceController.php

This will:
1. Load all active leave types
2. Fetch leave balances for the current employee and year from the migrated Excel data
3. Display them automatically in leaveCreditsTab.blade.php
